// ignore: file_names
import 'package:flutter/material.dart';

class InsectsScreen extends StatelessWidget {
  const InsectsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Insects Screen'),
    );
  }
}
